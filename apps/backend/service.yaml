apiVersion: v1
kind: Service
metadata:
  name: ecom-backend
  namespace: ecom
spec:
  selector:
    app: ecom-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
